services:
  mongo:
    image: mongo:7
    container_name: election-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - election_mongo_data:/data/db

volumes:
  election_mongo_data:
